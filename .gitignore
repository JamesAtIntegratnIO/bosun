# `go build` with no -o drops the binary next to the source.
/bosun

# helm package output
*.tgz

# desloppify local state
.desloppify/

# desloppify scorecard, regenerated on every scan
scorecard.png
